{
  "name": "uno-arena",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "test:engine": "vitest run packages/engine",
    "build:web": "npm run build -w apps/web"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "firebase": "^12.14.0"
  }
}
